{
  "scripts": {
    "start": "SLS_OFFLINE=true sls offline --stage local --disableCookieValidation --verbose"
  },
  "devDependencies": {
    "esbuild": "^0.17.10",
    "serverless": "^3.27.0",
    "serverless-deployment-bucket": "^1.6.0",
    "serverless-esbuild": "^1.39.0",
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.262.0",
    "@aws-sdk/client-ses": "^3.262.0"
  }
}
